# list_view_bloc_example

Example project with list view and entries.

The entries retain their state, even if they are no longer visible. This works by keeping the cubits in a list and closing them manually.
